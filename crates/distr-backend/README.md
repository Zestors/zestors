# zestors-distr-backend

The transport interface of [zestors-distr](https://github.com/Zestors/zestors) clusters: the `Backend` traits, `NodeId` and `NodeAddr`. Implement `Backend` to run a cluster over another network; `zestors-distr-quic` is the QUIC implementation.
